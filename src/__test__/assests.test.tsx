import {
  HashedlnLogo,
  ProfileLogo,
  shopingCart,
  ReactLogo,
  WishListLogo,
  WishListLogoActive,
  ArrowLogo,
  SearchIcon,
  GoToCheckOut,
  PopupHeader,
  PopupCircle,
  PaginationArrow,
  PaginationArrow2,
  DeleteIcon,
  Clock,
  AddtoCart,
  wishlist,
  alreadyExist,
} from '../assests/index';
describe('Logo Imports', () => {
  it('should import HashedlnLogo', () => {
    expect(HashedlnLogo).toBeDefined();
  });
  it('should import ProfileLogo', () => {
    expect(ProfileLogo).toBeDefined();
  });
  it('should import shopingCart', () => {
    expect(shopingCart).toBeDefined();
  });
  it('should import ReactLogo', () => {
    expect(ReactLogo).toBeDefined();
  });
  it('should import WishListLogo', () => {
    expect(WishListLogo).toBeDefined();
  });
  it('should import WishListLogoActive', () => {
    expect(WishListLogoActive).toBeDefined();
  });
  it('should import ArrowLogo', () => {
    expect(ArrowLogo).toBeDefined();
  });
  it('should import SearchIcon', () => {
    expect(SearchIcon).toBeDefined();
  });
  it('should import GoToCheckOut', () => {
    expect(GoToCheckOut).toBeDefined();
  });
  it('should import PopupHeader', () => {
    expect(PopupHeader).toBeDefined();
  });
  it('should import PopupCircle', () => {
    expect(PopupCircle).toBeDefined();
  });
  it('should import PaginationArrow', () => {
    expect(PaginationArrow).toBeDefined();
  });

  it('should import PaginationArrow2', () => {
    expect(PaginationArrow2).toBeDefined();
  });

  it('should import DeleteIcon', () => {
    expect(DeleteIcon).toBeDefined();
  });

  it('should import Clock', () => {
    expect(Clock).toBeDefined();
  });

  it('should import AddtoCart', () => {
    expect(AddtoCart).toBeDefined();
  });

  it('should import wishlist', () => {
    expect(wishlist).toBeDefined();
  });

  it('should import alreadyExist', () => {
    expect(alreadyExist).toBeDefined();
  });
});
